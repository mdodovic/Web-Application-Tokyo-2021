import { TreeCollection } from "./treeCollection";

export class GroupPhase {
    sportName: string;
    gender: string;
    groupNumber: number;
    roundNumber: number;

    groupMatches: Array<TreeCollection>;
}